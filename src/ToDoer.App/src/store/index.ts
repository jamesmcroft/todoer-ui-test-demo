import { createStore } from 'vuex';

/**
 * Defines the application's Vuex store and registered domain-specific modules/stores.
 */
const store = createStore({
  state() {
  },
  modules: {
  }
})

export default store;
